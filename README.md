# Go-WhatsApp-Bot

A dimple WhatsApp Bot based on WhatsMeow

Rename `.env.example` to `.env`:

```bash
mv .env.example .env
```

Install deps and run:

```bash
go mod tidy
go run main.go
```
